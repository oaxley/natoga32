# -*- coding: utf-8 -*-
# vim: filetype=python
#
# This source file is subject to the MIT License
# that is bundled with this package in the file LICENSE.txt.
# It is also available through the Internet at this address:
# https://opensource.org/license/mit
#
# @author	Sebastien LEGRAND
# @license	MIT License
#
# @brief	Compiler token class

# ----- imports
from __future__ import annotations
from typing import Any, Dict, List, Optional, Union

from dataclasses import dataclass

from packages.specs import TokenType


#----- classes
@dataclass
class Token:
    """Simple class to represent a token generated by the lexer

    Members:
    - type (TokenType): the type of the token as defined in TokenTypes class
    - value (str): the value of the token as seen in the assembly file
    - row (int): the line where the token appears
    - col (int): the position in the line where the token appears
    """
    type: TokenType = TokenType.UNKNOWN
    value: str = ""
    row: int = 0
    col: int = 0

    def __repr__(self) -> str:
        #return f"{self.type.name:>12} | ({self.row:3},{self.col:3}) | {self.value:10}"
        return f"{self.type.name};{self.row};{self.col};{self.value}"


class TokenStream:
    """Class to hold a stream of tokens"""
    def __init__(self, tokens: List[Token]) -> None:
        """Constructor"""
        self.tokens = tokens
        self.pos = 0

    def peek(self, inc: int = 0) -> Optional[Token]:
        """Return the next token, without consuming it

        Args:
            inc (int): the increment to look beyond the next token (default:0)

        Returns:
            Optional[Token]: None if we are beyond boundaries, the next token otherwise
        """
        idx = self.pos + inc
        if idx >= len(self.tokens):
            return None
        if idx < 0:
            return None
        return self.tokens[idx]

    def advance(self) -> Optional[Token]:
        """Return the current token and move the cursor forward to the next token

        Returns:
            Optional[Token]: None if no token can be found, the current token otherwise
        """
        token = self.peek()
        if token is None:
            return None
        self.pos += 1
        return token

    def expect(self, what: Union[TokenType, str]) -> bool:
        """Check the current token against conditions

        - If what is a TokenType, token is tested against its type.
        - If what is a str, token is tested against its value.

        Args:
            what (Union[TokenType, str]): condition to test the current token against

        Returns:
            bool: True if the current token satisfies all the conditions, False otherwise
        """
        token = self.peek()
        if token:
            if isinstance(what, TokenType) and token.type == what:
                return True
            elif isinstance(what, str) and token.value == what:
                return True

        return False

    def at_end(self) -> bool:
        """Check if we are at the end of the token stream

        Returns:
            bool: True if we are at the end of the stream, False otherwise
        """
        token = self.peek()
        if token is None:
            return True
        elif token.type == TokenType.EOF:
            return True
        else:
            return False
